services:
  - type: web
    name: budget-tracker
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn wsgi:app --timeout 60 --workers 1 --worker-class sync --max-requests 50 --max-requests-jitter 5 --keep-alive 2 --preload --bind 0.0.0.0:10000
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13
      - key: FLASK_ENV
        value: production
      - key: SQLALCHEMY_ENGINE_OPTIONS
        value: '{"pool_size": 1, "pool_recycle": 1800, "pool_pre_ping": true, "max_overflow": 1, "pool_timeout": 10}' 