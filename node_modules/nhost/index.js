#!/usr/bin/env -S node --trace-uncaught --expose-gc --unhandled-rejections=strict
var HOST_IP;

import loads from '@3-/nt/loads.js';

HOST_IP = loads(process.env.HOSTS);

export default (host) => {
  return HOST_IP[host] || host;
};
